version: 0.2

env:

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - yarn
  pre_build:
    commands:
      - echo Nothing to do in the pre_build phase... Maybe add tests
  build:
    commands:
      - yarn build
  post_build:
    commands:
      - yarn version patch
artifacts:
  files:
    - bin/**/*
  name: authenticator/${yarn --silent current-version}/
